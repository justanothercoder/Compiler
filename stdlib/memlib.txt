def unsafe sbrk(int size) : void*
{
	void* break = __brk(null)
	
	__brk(break + size);

	return break;
}

def unsafe mmap(int size) : void* {
    return __mmap(0, size, 3, 34, 0-1, 0)
}

/*
template <class T>
def unsafe malloc(int size) : T*
{
	return sbrk(size)
}
*/
